{
  "version": 2,
  "buildCommand": "rm -rf node_modules/.cache && npm install && npm run build",
  "outputDirectory": "dist/sms-frontend",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

